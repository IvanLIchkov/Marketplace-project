spring:
  jpa:
    hibernate:
      ddl-auto: update
    defer-datasource-initialization: true
  sql:
    init:
      mode: never
  mvc:
    hidden-method:
      filter:
        enabled: true

logging:
  level:
    org.hibernate.SQL: DEBUG
    org.hibernate.orm.jdbc.bind: TRACE

mail:
  marketplace: "mobilele@example.com"
  host: localhost
  port: 3333
  username: test@example.com
  password: test

#  jdbc:
#    hsqldb:
#      mem:
#        testdb;
#  sql.
#    syntax_mys=true.